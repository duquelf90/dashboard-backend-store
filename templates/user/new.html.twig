{% extends 'base.html.twig' %}
{% import 'components/buttons.html.twig' as buttons %}
{% block title %}
    New User
{% endblock %}

{% block body %}
    <div class="flex items-center justify-between">
        {{ include('components/title.html.twig', {'txt_label': 'Nuevo Usuario'}) }}
        {{ buttons.button_back(path('app_user_index')) }}
    </div>
    {{ include('user/_form.html.twig', {'button_label': 'Agregar'}) }}

{% endblock %}
