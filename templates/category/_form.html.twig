{% import 'components/button/index.html.twig' as buttons %}

<div class="px-4 py-3 mb-8 bg-white rounded-lg shadow-md dark:bg-gray-800">
    {{ form_start(form) }}

    <label class="block text-sm">
        <span class="text-gray-700 dark:text-gray-400 my-2">
            Nombre
        </span>
        {{ form_widget(form.name, { 'attr': {'class': 'block w-full mt-1 text-sm my-2 dark:border-gray-600 dark:bg-gray-700 focus:border-purple-400 focus:outline-none focus:shadow-outline-purple dark:text-gray-300 dark:focus:shadow-outline-gray form-input', 'placeholder': 'Nombre de la categoria'} }) }}
    </label>

        <div class="mt-4 text-sm">
            {{ buttons.button_submit(button_label) }}
        </div>

        {{ form_end(form) }}

</div>
